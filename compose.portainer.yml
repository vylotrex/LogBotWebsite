version: "3.8"
services:
  web:
    image: ghcr.io/vylotrex/LogBotWebsite:latest
    ports:
      - "8080:80"
    restart: unless-stopped
    # Enable if you’ll use Watchtower (optional):
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
